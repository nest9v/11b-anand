let x=10,y=20; //utga ugh
function min(x,y){ //uildel hiih
    if(x<y){ //hamgiin baga 
        return x;
    }else{
        return y;
    }
}

const result = min(x , y); //min aa zarlah
console.log(result); //hariu
